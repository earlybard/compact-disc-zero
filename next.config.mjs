/** @type {import('next').NextConfig} */
const nextConfig = {
    // This builds as a full SPA. We may not want this if we do more advanced things like auth/dbs in future.
    // output: "export"
};

export default nextConfig;
